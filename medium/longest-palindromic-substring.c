/*
 * LeetCode Solution: Longest Palindromic Substring
 * Difficulty: Medium
 * Language: c
 * URL: https://leetcode.com/problems/longest-palindromic-substring/submissions/1976495195/
 * Date: 12/04/2026, 20:30:48
 * Solution: Initial
 */

maxLen = right - left + 1;
                    start = left;
                if (right - left + 1 > maxLen) {
            while (left >= 0 && right < s.size() && s[left] == s[right]) {
            int left = i, right = i;
        for (int i = 0; i < s.size(); i++) {
                }
                left--;
                right++;
            }